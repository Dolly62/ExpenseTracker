import React, { Fragment, useContext } from "react";
import classes from "./Password.module.css";

const Password = () => {

  return (
    <Fragment>
      <button className={classes.passsty}>Forget Passwod</button>
    </Fragment>
  );
};

export default Password;
